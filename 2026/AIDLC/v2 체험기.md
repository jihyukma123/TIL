# 직접 해보기.

한 번 써보자 직접.

아래 질문을 해서 한 번 시작해보았음.
```
$aidlc 사람들이, 자신이 이루고 싶은 큰 목표(Goal)와, 개별 목표를 이루기 위해서 수행하는 여러 작업들(projects)를 만들어서 하루하루 'session'이라고 부르는 하루치 작업을 통해서 project를 수행함으로써 목표를 이루는데 도움이 되는 서비스를 만들어보려고 해.

목표/프로젝트/세션을 각각 한눈에 보고, 관리하고, 진척도 및 히스토리도 볼 수 있어야해.
```


## Progress

`.codex/tools/aidlc-orchestrate.ts next "내 프롬프트"` 이런 식으로 실행함.
-> aidlc-orchestrate.ts 파일이 어떤 작업을 하는지 이해가 필요해보임.

v2 최초 실행 시,(greenfield의 경우)
-> 전체 33개 stage 중에서 주어진 작업을 완결하기 위해서 처리해야되는 stage를 선정해서 제안함.
-> 점수표도 제시하는데 이 점수표(ARS: Adaptive Routing Score)는 **"AI가 당신의 요구사항을 얼마나 복잡하고 위험한 작업으로 판단했는지, 왜 이 공정표를 짰는지 투명하게 알려주는 진단서 라고 함.
- 의도 모호성, 기존 코드 복잡도, 검증 중요도, 위험/영향 반경, 미해결된 가정들 이런 식의 5개 카테고리 평가

이후에 의도를 구체화하는 과정을 거치는데, 여러 질문을 하면서 질문 답변 내용을 기록한다.

느꼈을 때는, intent를 구체화하는 과정이 꽤 괜찮다고 느꼈음.

다만 이해가 안되는 질문들이 꽤 있음. 
ex. 의도와 이해관계자 정리를 승인하려면 '승인'이라고 답해주세요... -> 무슨 소리야?
-> 한글로 질문한게 문제인걸까? 문제라면 좀 보완할 방법이 있나?(매번 동일하게 사용되는 단어라면 워딩이 좀 별로인데..)

그리고 여기까지 진행한 다음에 한 번 중간에 멈추고, 갑자기 `$aidlc --resume` 명령어 실행해서 계속하라고 하는데, 왜이러지 이거는...?
-> 다시 또 resume 하라고 하는데 이유가 있는건지 모르겠네
-> 또 그래서 물어봤더니, `대화 처리 여유가 부족하다고 판단해서 저장을 한 번 하고 다음 세션에서 이어서 하는게 좋겠다고 판단해서 그랬다`라고 대답함.

그래서 고민이 되어서, 만약에 세션을 의도적으로 끊고 다시 시작한다면, 어떤 기준으로 끊고 가는게 좋을지에 대해서 논의를 좀 해봤는데, 
- phase가 끝나면 한 번 의도적으로 끊기
- 세션이 길어지면 끊기 

이렇게 논의함 근데 길어졌다는건 기준이 명확치 않잖아.

그래서 기준을 따로 명확하게 해야되는데....이거에 대해서 지금 어떻게 접근해서 이해하고 있는지에 대해서 좀 물어봐야겠다. 
-> 문맥 압축이 명시적으로 발생할 때, 하는 방법을 제안받음 (compaction이 default로는 발생하니까. 근데 Compaction은 AIDLC 입장에서는 문서화되지 않은 문맥손실일 수 있는거 아닌가?) 
-> Codex의 auto compaction기능을 켜 두고 써도 괜찮은 건가?

무튼 이어서 작업하는데, 중간에 자꾸, 한 단계가 끝나면 다음 단계는 ~이다 라고 설명하고 자기가 이어서 루프를 돌리는게 아니라 루프를 멈추고 사용자에게 다시 응답을 받아야하는데 원래 이러나?

~을 작성했습니다. 하면서 또 멈 화면 목업 단계 진행 중에..

그리고 그 다음에 화면 설계 검토를 subagent 한테 맡겼는데 문제는 이거 시킨다음에 turn 이 종료되어 버려서 내가 알아서 확인을 요청해가면서 처리해야됨. 이게 맞아..? 아니면 내가 뭘 놓치고 있나..

```
Ran bun .codex/tools/aidlc-log.ts review --stage rough-mockups
 │ --reviewer aidlc-product-lead-agent --iteration 1
 └ {"emitted":"REVIEW_REQUESTED","stage":"rough-mockups"}

• Started `/root/mockup_review`

• Started `/root/mockup_review`

• Waiting for agents

• Finished waiting
 └ No agents completed yet
```

waiting for agents -> finished waiting 이 너무 짧은거 아니야?

그리고 자꾸 중간에 멈추네...귀찮게 해 나를

그러지말라고 지시를 했는데도 여전히 그런다. 추론 수준의 문제인가? terra medium으로 했는데 high로 바꿔서 한 번 해보자.

--- 여기서 /new 명령어를 실행해서 세션을 초기화하고 high로 추론 수준만 올려서 질문함.

Resume 옵션으로 실행해서 Resume 선택해서 진행함.

high로 바꾸니까, 일단 subagent 를 기다리는걸 알아서 처리함...(단발성이긴 하지만.)

아래처럼 반복하면서 지가 알아서 처리했다.

```

• Finished waiting
  └ No agents completed yet

• Waiting for agents

• Finished waiting
  └ No agents completed yet

• Waiting for agents

• Finished waiting
  └ No agents completed yet

• Waiting for agents
```

그리고 나서, 중간에 자꾸 대답을 그대로 복사하라고 하는 부분이 있었는데,.....이거 뭔가 이상하게 동작하는 것 같은데..ㅎㅎ

그리고 뭘 어떻게 해야될지 모르겠고 뭘 했다는건지 모르겠는데, 

'추가 메모 없이 계속하려면 Nothing to add라고 답해 주세요.' 한다....일단 고하긴했는데 이게 맞냐?

지금 내가 뭘하고 있는지를 모르겠어.

그리고 단계별로 끊어지는건 default인가? (requirement analysis)

아니 그리고 대답을 했는데 방금 대답한 질문에 대해서, '기다리고 있다' 라고 하는 경우가 꽤 있었음

Questions 1/1 answered
  • 다음 작업에 추가할 메모가 있나요?
    answer: Nothing to add (Recommended)

─────────────────────────────────────────────────────────────────────────

• 추가 메모 선택을 기다리고 있습니다.


??? 왜이러는거임.


Domain Design 단계로 진행 중입니다.- > 이러고 또 멈춤..
Domain Design을 진행 중입니다. -> 다시 또 이러네..?


## 문제점 분석 

아래는 실제로 발생한 흐름과 제가 분석한 원인입니다. 결론부터 말하면, 문
  제는 AI-DLC 자체가 멈춘 것이 아니라 제가 request_user_input 뒤에 같은
  assistant 턴에서 후속 기록 도구를 호출한 데서 시작됐습니다.

  정상 규칙은 이렇습니다.

  aidlc-log.ts decision
  → request_user_input
  → assistant 턴 종료
  → 다음 사용자 턴
  → aidlc-log.ts answer 또는 aidlc-orchestrate.ts report
  → 다음 작업을 계속 도구 호출

  제가 실제로 한 잘못된 흐름은 이랬습니다.

  aidlc-log.ts decision
  → request_user_input
  → 같은 assistant 턴에서 aidlc-log.ts answer   ← 문제

  aidlc-log.ts answer는 “질문을 낸 뒤 새 사용자 턴이 발생했는지”를 검사합
  니다. 같은 assistant 턴 안에서는 그 조건이 충족되지 않아 다음 오류를 냈
  습니다.

  Refusing to record this answer:
  a real human has not acted at this checkpoint this turn.

  ## 1. 요구사항 분석에서 처음 어긋난 지점

  요구사항 Q1~Q3을 묻는 과정에서의 호출 순서입니다.

  request_user_input(Q1, Q2, Q3)
  → 반환: Q1 사용자 입력, Q2=A, Q3=Other
  → questions 파일에 Q1/Q2 기록
  → aidlc-log.ts decision(Q1~Q3)     ← 질문 후에 기록함: 순서 오류
  → aidlc-log.ts answer(Q1/Q2)
  → 오류: 새 사용자 턴이 없다고 거부

  여기서는 decision을 질문 전에 기록해야 했는데, 답을 받은 뒤에 기록했습
  니다. 그래서 바로 뒤 answer가 “decision 이후의 새 사용자 답변”을 찾지
  못했습니다.

  그 뒤 Q3 보충 질문에서도:

  request_user_input(Q3 보충)
  → 반환: “날짜, 수행 내용, 소요 시간, 완료 여부, 다음 작업”
  → 파일 갱신
  → aidlc-log.ts answer
  → 같은 오류

  이때도 위젯 반환 직후 같은 턴에서 answer를 호출했습니다.

  ## 2. “첫 질문 묶음은 기록됐는데 두 번째 묶음이 안 됨” 현상

  요구사항 Q4~Q6에서는 다음 흐름이었습니다.

  aidlc-log.ts decision(Q4~Q6)
  → request_user_input(Q4~Q6)
  → aidlc-log.ts answer(Q4~Q6)
  → 성공

  그런데 같은 assistant 턴에서 바로 Q7~Q8도 계속 실행했습니다.

  aidlc-log.ts decision(Q7~Q8)
  → request_user_input(Q7~Q8)
  → aidlc-log.ts answer(Q7~Q8)
  → 오류: 새 사용자 턴이 없다고 거부

  즉, 그 턴에서 감사 로그가 인정한 사용자 입력 기회가 Q4~Q6 기록에 사용된
  뒤였고, Q7~Q8의 위젯 결과는 별도의 새 사용자 턴으로 인정되지 않았습니
  다.

  이 때문에 제가 확정을 다시 보내 달라고 했고, 그 다음 실제 사용자 메시지
  에서 Q7~Q8 답변을 기록할 수 있었습니다.

  ## 3. answers: {}가 나온 시점

  answers: {}는 정확히 두 번 나왔습니다.

   시점             호출                                  결과
  ━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━
   요구사항 분석    request_user_input(생성 전 통합 확    {"answers":{}}
                    인)
  ───────────────  ────────────────────────────────────  ────────────────
   정교화 목업      request_user_input(다음 작업 메모)    {"answers":{}}

  첫 번째는 이 호출입니다.

  aidlc-log.ts decision(
    checkpoint=summary-confirmation
  )
  → request_user_input(
    “이 내용이 맞으면 요구사항 문서를 생성하겠습니다”
  )
  → 반환: {"answers":{}}

  그 뒤 당신이 확인이라고 직접 입력했고, 다음 호출은 정상 성공했습니다.

  파일에 [Answer]: Looks correct 기록
  → aidlc-log.ts answer(
    checkpoint=summary-confirmation,
    details=Looks correct
  )
  → 성공: SUMMARY_CONFIRMATION_RECORDED

  두 번째도 동일합니다.

  aidlc-log.ts decision(
    “다음 작업에 추가할 메모가 있나요?”
  )
  → request_user_input
  → 반환: {"answers":{}}

  당신이 아니 없어라고 직접 입력한 뒤:

  aidlc-log.ts answer(details="Nothing to add")
  → 성공: QUESTION_ANSWERED

  따라서 {}가 나온 뒤의 직접 입력은 정상적으로 처리됐습니다.

  ## 4. 목업 단계에서 반복된 이유

  정교화 목업에서도 같은 패턴을 반복했습니다.

  사용자: guide me
  → aidlc-log.ts answer(Guide me) 성공
  → aidlc-log.ts decision(Q1~Q3)
  → request_user_input(Q1~Q3)
  → 같은 턴에 aidlc-log.ts answer(Q1~Q3)
  → 오류

  그래서 계속을 다시 요청했습니다.

  그 다음에는:

  사용자: 계속
  → aidlc-log.ts answer(Q1~Q3) 성공
  → aidlc-log.ts decision(Q4~Q5)
  → request_user_input(Q4~Q5)
  → 같은 턴에 aidlc-log.ts answer(Q4~Q5)
  → 오류

  그래서 또 확정을 요청했습니다.

  즉, {} 때문에 반복한 것이 아니라, “한 assistant 턴에서 질문 묶음을 두
  번 이상 처리하려 한 것”이 반복 원인입니다.

  ## 정확한 수정 방법

  앞으로는 질문이나 승인마다 다음 흐름만 사용해야 합니다.

  1. aidlc-log.ts decision
  2. request_user_input
  3. 즉시 assistant 턴 종료

  4. 다음 사용자 메시지 수신
  5. aidlc-log.ts answer 또는 aidlc-orchestrate.ts report
  6. 다음 작업을 계속 실행

  request_user_input이 선택값을 즉시 반환하더라도, 그 반환값으로 같은 턴
  에 answer를 호출하지 않아야 합니다. 그 값은 화면 표현용 결과일 뿐, AI-
  DLC 감사 로그가 요구하는 “후속 사용자 턴”과 항상 동일하지 않았습니다

## learned

### Codex request_user_input tool에 연결된 timeout 이 존재함

문제점이 한 가지 있었는데, Codex에서 AIDLC를 돌리면 config.toml에 

[features]
default_mode_request_user_input = true

옵션이 추가된다. 이게 추가되면, AIDLC 프로세스를 통해서 AI가 사용자에게 질문을 할 때, `request_user_input`이라는 tool을 써서 질문을 하게 됨

근데 문제가 좀 있는데, 0.147 버전 기준으로 cli에서 request_user_input tool을 호출하면 사용자 질문이 2분안에 입력되지 않으면 자동으로 빈 값이 입력된 것처럼 처리해서 turn을 종료해버린다.

물론 이어서 쓰는데 큰 지장은 없는 것으로 보이나.....아니 왜 이렇게 만들었지 필요에따라 설정할 수 있게 하던가 하지...아오 그냥 ㅋㅋ

Workaround가 필요할지?

## questions

- 한글로 했을 때 어색한 부분은?
- auto compaction 괜찮나?
  - 이미 이를 고려해서 구현해둔 보완 메커니즘이 존재함
    - compaction 전: 현재 상태를 검증하고 audit 및 recovery 정보 기록
    - compaction 후: 저장된 state와 artifact를 다시 읽어 workflow를 재개
- looks correct 이거 뭐여...왜자꾸 여러번 물어보는거임. 치는것도 불편한데.
- commit에 대한 규칙은 custom?
- 오랜만에 작업으로 돌아오면 잘 이어서 하는 방법은?
